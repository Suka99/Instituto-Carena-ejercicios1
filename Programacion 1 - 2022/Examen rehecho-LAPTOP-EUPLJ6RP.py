# Definir una función insertar_valor(lista, valor)
def insertar_valor(lista, valor):
# Recibe como argumento una lista ordenada de enteros.
    elementos = []
# En nueva_lista insertar los elementos de lista y el valor en la posición que corresponda por su orden (puede usar una variable booleana, un for un if y append).
for valor in elementos:
    if valor not
# Retornar nueva_lista.
    return nueva_lista